import React from "react";
import { twMerge } from "tailwind-merge";
type PostType = {
  title: string;
};
export const PostCard = ({ post }: { post: any }) => {
  return (
    <div>
      <div>hi</div>
    </div>
  );
};
